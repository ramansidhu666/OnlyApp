@model IEnumerable<CommunicationApp.Entities.Case>

@if (Model != null)
{
    <div class="grid">
        <table cellspacing="0" width="80%">
            <thead>
                <tr>
                    <th>
                        Name
                    </th>
                    <th>
                        Age
                    </th>
                    <th>
                        Address
                    </th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {

                    @Html.ActionLink("Edit", "Edit", new { id = item.CaseID }, new { @class = "fa fa-edit" })
                    
                    @*<tr>
                        <td align="left">
                            @item.Name
                        </td>
                        <td align="left">
                            @item.Age
                        </td>
                        <td align="left">
                            @item.Address
                        </td>

                    </tr>*@
                }
            </tbody>
        </table>

    </div>

}


